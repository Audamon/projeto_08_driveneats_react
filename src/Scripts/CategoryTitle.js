export default function CategoryTitle(props){
    return(
        <div class="category-title font-righteous font-size-26">
            {props.dishTitle}
            {/* Primeiro, seu prato */}
        </div>
    );
}